class Worker {
  final String name;
  final String imageUrl;
  final double rating;
  final String type; // Asegúrate de tener esta propiedad

  Worker({
    required this.name,
    required this.imageUrl,
    required this.rating,
    required this.type, // Asegúrate de inicializar esta propiedad
  });
}
